# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# ── uv / virtualenv ──
.venv/
venv/
env/

# ── Pipeline artifacts (regenerated by running the pipeline) ──
outputs/

# ── Docs ──
docs/

# ── Airflow local state (docker compose) ──
airflow/logs/
airflow/plugins/
logs/
airflow.db
airflow.cfg
standalone_admin_password.txt

# ── OS / editor ──
.DS_Store
.idea/
.vscode/

# ── Secrets — never commit ──
.env
*.pem
credentials.json
